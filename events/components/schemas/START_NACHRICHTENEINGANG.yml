description: "Schema für Nachrichteingang | Schema for the inbound message"
properties:
    eventName:
      $ref: items/eventName.yml
    id:
      $ref: items/id.yml
required:
  - id
  - eventName
type: object
