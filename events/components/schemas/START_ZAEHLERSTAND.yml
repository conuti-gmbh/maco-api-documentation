START_ZAEHLERSTAND:
  description: "Schema f\xFCr Zaehlerstand | Schema for the Zaehlerstand"
  properties:
    eventName:
      default: START_ZAEHLERSTAND
      type: string
    id:
      description: BelegId des Datensatzes in der Tabelle im Backend | DocumentId
        of the data record in the table in the backend
      example: '110000000605'
      type: string
  required:
  - id, eventName
  type: object
