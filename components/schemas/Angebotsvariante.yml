properties:
  angebotsstatus:
    $ref: 'Angebotsstatus.yml'
  beschreibung:
    type: string
  bindefrist:
    format: date-time
    type: string
  erstelldatum:
    format: date-time
    type: string
  gesamtkosten:
    $ref: 'Betrag.yml'
  gesamtmenge:
    $ref: 'Menge.yml'
  teile:
    items:
      $ref: 'Angebotsteil.yml'
    type: array
type: object
