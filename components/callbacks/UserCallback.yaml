UserCallback:
  post:
    operationId: userPostCallback
    summary: summary
    requestBody:
      description: Callback payload
      content:
        application/json:
          schema:
            type: object
            properties:
              id:
                type: integer
                format: int64
              name:
                type: string
              email:
                type: string
                format: email
    responses:
      '200':
        description: Callback successful
