PI55662:
  description: "55662 MSB\u2192weiterer MSB \xC4nderung Daten der Tranche"
  properties:
    AN:LESENDE_API:
      description: LESEN_TRANCHE_BASIS
      enum:
      - weiterer MSB
      type: string
    AN:SCHREIBENDE_API:
      description: AKTUALISIEREN_TRANCHE_BASIS
      enum:
      - weiterer MSB
      type: string
    VON:LESENDE_API:
      description: LESEN_TRANCHE_BASIS
      enum:
      - MSB
      type: string
    VON:SCHREIBENDE_API:
      description: n.v.
      enum:
      - MSB
      type: string
    VON:TRIGGER_EVENT:
      description: AENDERUNG_SD
      enum:
      - MSB
      type: string
  type: object
