PI13002:
  description: "13002 LF\u2192NB Z\xE4hlerstand (Gas)"
  properties:
    AN:LESENDE_API:
      description: LESEN_MARKTLOKATION_BASIS, LESEN_MESSLOKATIONS_BASIS, LESEN_NETZLOKATION_BASIS,
        LESEN_TRANCHE_BASIS
      enum:
      - NB
      type: string
    AN:SCHREIBENDE_API:
      description: ERSTELLEN_ENERGIEMENGE_ZAEHLERSTAND
      enum:
      - NB
      type: string
    VON:LESENDE_API:
      description: LESEN_ENERGIEMENGE_ZAEHLERSTAND
      enum:
      - LF
      type: string
    VON:SCHREIBENDE_API:
      description: "R\xFCckgabe DAR falls notwendig"
      enum:
      - LF
      type: string
    VON:TRIGGER_EVENT:
      description: START_ZAEHLERSTAND
      enum:
      - LF
      type: string
  type: object
