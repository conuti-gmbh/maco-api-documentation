PI25001:
  description: "25001 NB\u2192MSB (entspricht dem MSB am Objekt Messlokation) Berechnungsformel"
  properties:
    AN:LESENDE_API:
      description: ''
      enum:
      - MSB (entspricht dem MSB am Objekt Messlokation)
      type: string
    AN:SCHREIBENDE_API:
      description: ''
      enum:
      - MSB (entspricht dem MSB am Objekt Messlokation)
      type: string
    VON:LESENDE_API:
      description: ''
      enum:
      - NB
      type: string
    VON:SCHREIBENDE_API:
      description: ''
      enum:
      - NB
      type: string
    VON:TRIGGER_EVENT:
      description: ''
      enum:
      - NB
      type: string
  type: object
