PI55001:
  description: "55001 LFN\u2192NB Anmeldung verb. MaLo"
  properties:
    AN:LESENDE_API:
      description: 'LESEN_MARKTLOKATION_BASIS, LESEN_MESSLOKATION_BASIS, LESEN_BILANZIERUNG_BASIS,
        LESEN_ENERGIELIEFERVERTRAG_BASIS, LESEN_NETZNUTZUNGSVERTRAG_BASIS, '
      enum:
      - NB
      type: string
    AN:SCHREIBENDE_API:
      description: ERSTELLEN_PROZESSDATEN_LIEFERBEGINN
      enum:
      - NB
      type: string
    VON:LESENDE_API:
      description: LESEN_MARKTLOKATION_BASIS
      enum:
      - LFN
      type: string
    VON:SCHREIBENDE_API:
      description: ''
      enum:
      - LFN
      type: string
    VON:TRIGGER_EVENT:
      description: START_LIEFERBEGINN
      enum:
      - LFN
      type: string
  type: object
