PI55051:
  description: "55051 MSB (entspricht MSBA am Objekt Messlokation)\u2192NB Ende MSB"
  properties:
    AN:LESENDE_API:
      description: ''
      enum:
      - NB
      type: string
    AN:SCHREIBENDE_API:
      description: ''
      enum:
      - NB
      type: string
    VON:LESENDE_API:
      description: ''
      enum:
      - MSB (entspricht MSBA am Objekt Messlokation)
      type: string
    VON:SCHREIBENDE_API:
      description: ''
      enum:
      - MSB (entspricht MSBA am Objekt Messlokation)
      type: string
    VON:TRIGGER_EVENT:
      description: ''
      enum:
      - MSB (entspricht MSBA am Objekt Messlokation)
      type: string
  type: object
