PI55039:
  description: "55039 MSB (entspricht MSBN am Objekt Messlokation)\u2192MSB (entspricht\
    \ MSBA am Objekt Messlokation) K\xFCndigung MSB"
  properties:
    AN:LESENDE_API:
      description: ''
      enum:
      - MSB (entspricht MSBA am Objekt Messlokation)
      type: string
    AN:SCHREIBENDE_API:
      description: ''
      enum:
      - MSB (entspricht MSBA am Objekt Messlokation)
      type: string
    VON:LESENDE_API:
      description: ''
      enum:
      - MSB (entspricht MSBN am Objekt Messlokation)
      type: string
    VON:SCHREIBENDE_API:
      description: ''
      enum:
      - MSB (entspricht MSBN am Objekt Messlokation)
      type: string
    VON:TRIGGER_EVENT:
      description: ''
      enum:
      - MSB (entspricht MSBN am Objekt Messlokation)
      type: string
  type: object
