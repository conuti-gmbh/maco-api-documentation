get:
  description:
    "Ändert einen Netznutzungsvertrag mittels LokationsId  (Parameter1)\
    \  zum Zeitpunkt (Parameter2) | Changes a network usage contract using LocationId\
    \ (Parameter1) at time (Parameter2)"
  operationId: LESEN_MESSLOKATIONS_BASIS
  parameters:
    - description: Name der Operation | Name of the operation
      in: query
      name: command
      required: true
      schema:
        default: LESEN_MESSLOKATIONS_BASIS
        example: LESEN_MESSLOKATIONS_BASIS
        type: string
    - description: LokationsId | Location ID
      in: query
      name: parameter1
      required: true
      schema:
        example: "74018657187"
        type: string
    - $ref: ../../components/parameters/parameter2.yml
  responses:
    "200":
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/v1/bo/Messlokation.schema.json"
      description:
        Erfolgreiches Lesen der Marktlokation | Successful reading of the
        market location
    "400":
      content:
        application/json:
          schema:
            $ref: "../../components/responses/ERROR_RESPONSE.yml"
      description: Fehlerhafte Anfrage | Bad Request
  summary: LESEN_MESSLOKATIONS_BASIS
  tags:
    - LESEN | READ
