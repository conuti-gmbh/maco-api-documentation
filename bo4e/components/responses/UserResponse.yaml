description: A list of users
content:
  application/json:
    schema:
      type: array
      items:
        $ref: "../schemas/User.yaml"
    examples:
      UserExample:
        $ref: "../examples/UserExample.yaml"
